/* frontend/psx/vsprintf.cpp -- RECONSTRUCTED (empty runtime/library TU).
 *   FRONTEND\PSX\vsprintf.obj -- empty marker (vsprintf @0x80051260 sz=0 front.text). The real vsprintf lives in PsyQ libc (syslib campaign); this TU contributes nothing.
 *   Verified empty: no TUSummary row (or EXT=0/STAT=0) + ranges-map shows only a 0-4 byte
 *   PSYLINK boundary marker; no functions, no data attributed. Out of game-source scope
 *   (library/runtime), included for tree-inventory completeness. */
#include "vsprintf.h"
/* (intentionally empty) */
/* end of vsprintf.cpp */
