/* frontend/psx/memdsp.cpp -- RECONSTRUCTED debug TU (src MEMDSP.CPP).
 *
 *   memdsp.obj is a debug-build translation unit. no TUSummary row (empty SYM block)
 *   nfs4f_ranges: owns marker `memdsp` @0x80051260 in front.text (size 0, bare label).
 *   These debug globals are not referenced by the reconstructed game logic (the debug code
 *   paths that used them were not reconstructed); they are materialized here because the obj
 *   No code or data; stub records the finding for tree completeness.
 */
#include "memdsp.h"

/* (intentionally empty: no functions, no data) */
/* end of memdsp.cpp */
