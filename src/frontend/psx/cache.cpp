/* frontend/psx/cache.cpp -- RECONSTRUCTED (empty runtime/library TU).
 *   FRONTEND\PSX\cache.obj -- marker only (cache @0x8005125C sz=4 front.text, a 4-byte alignment word). Cache/scratch helper stub.
 *   Verified empty: no TUSummary row (or EXT=0/STAT=0) + ranges-map shows only a 0-4 byte
 *   PSYLINK boundary marker; no functions, no data attributed. Out of game-source scope
 *   (library/runtime), included for tree-inventory completeness. */
#include "cache.h"
/* (intentionally empty) */
/* end of cache.cpp */
