/* frontend/common/screengrid.cpp -- RECONSTRUCTED (ESSENTIALLY-EMPTY TU; emits no real code or data)
 *
 *   ScreenGrid.obj (src SCREENGRID.CPP) is an empty/stub translation unit: it contributes NO functions and
 *   NO data to the linked image. Verified three ways:
 *     (1) SYM FILE-block: no TUSummary row (empty SYM block).
 *     (2) nfs4f_ranges 0-gap whole-binary map: ScreenGrid.obj owns a single marker row
 *         `ScreenGrid` @0x80045950 in front.text, size 0 -- a zero-byte label only (the bytes at this VA are already the next obj's code).
 *     (3) no .text function bodies / no .data/.bss symbols are attributed to it.
 *
 *   The source file existed in the original tree (SCREENGRID.CPP) but compiled to nothing in this
 *   build -- a stubbed/disabled feature (header-only or #if 0). Any types it referenced are
 *   already in nfs4_types.h. This stub records the finding for tree completeness; the VA-
 *   descending walk skipped it because it has no .text range.
 */
#include "screengrid.h"
/* (intentionally empty: no functions, no data) */
/* end of screengrid.cpp */
