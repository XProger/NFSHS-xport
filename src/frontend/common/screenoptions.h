/* frontend/common/screenoptions.h -- header for the essentially-empty ScreenOptions translation unit.
 * ScreenOptions.obj contributes no real code/data (only a 0-byte PSYLINK module-boundary marker);
 * this header exists so screenoptions.cpp has a sibling include (per-TU header convention). */
#ifndef _FRONTEND_COMMON_SCREENOPTIONS_H_
#define _FRONTEND_COMMON_SCREENOPTIONS_H_
#include "../../nfs4_types.h"
#endif /* _FRONTEND_COMMON_SCREENOPTIONS_H_ */
