/* frontend/common/screendemo.cpp -- RECONSTRUCTED (ESSENTIALLY-EMPTY TU; emits no real code or data)
 *
 *   ScreenDemo.obj (src SCREENDEMO.CPP) is an empty/stub translation unit: it contributes NO functions and
 *   NO data to the linked image. Verified three ways:
 *     (1) SYM FILE-block: no TUSummary row (empty SYM block).
 *     (2) nfs4f_ranges 0-gap whole-binary map: ScreenDemo.obj owns a single marker row
 *         `ScreenDemo` @0x8004B7F4 in front.text, size 4 -- a single 0x00000000 alignment word (the next word, 0x27bd... , is the NEXT obj's function prologue).
 *     (3) no .text function bodies / no .data/.bss symbols are attributed to it.
 *
 *   The source file existed in the original tree (SCREENDEMO.CPP) but compiled to nothing in this
 *   build -- a stubbed/disabled feature (header-only or #if 0). Any types it referenced are
 *   already in nfs4_types.h. This stub records the finding for tree completeness; the VA-
 *   descending walk skipped it because it has no .text range.
 */
#include "screendemo.h"
/* (intentionally empty: no functions, no data) */
/* end of screendemo.cpp */
