/* game/psx/ddvfont.cpp -- RECONSTRUCTED (ESSENTIALLY-EMPTY TU; emits no real code or data)
 *
 *   Ddvfont.obj (src DDVFRONT.H) is an empty/stub translation unit: it contributes NO functions and
 *   NO data to the linked image. Verified three ways:
 *     (1) SYM FILE-block: TUSummary ARG=0 AUTO=0 REG=0 EXT=0 STAT=0 (87 type records: 9 STRTAG + 26 TPDEF + 43 MOS).
 *     (2) nfs4f_ranges 0-gap whole-binary map: Ddvfont.obj owns a single marker row
 *         `Ddvfont_func` @0x800BD3C0 in .text, size 0 -- a zero-byte label only (the bytes at this VA are already the next obj's code).
 *     (3) no .text function bodies / no .data/.bss symbols are attributed to it.
 *
 *   The source file existed in the original tree (DDVFRONT.H) but compiled to nothing in this
 *   build -- a stubbed/disabled feature (header-only or #if 0). Any types it referenced are
 *   already in nfs4_types.h. This stub records the finding for tree completeness; the VA-
 *   descending walk skipped it because it has no .text range.
 */
#include "ddvfont.h"
/* (intentionally empty: no functions, no data) */
/* end of ddvfont.cpp */
