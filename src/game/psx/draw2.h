/* game/psx/draw2.h -- header for the essentially-empty Draw2 translation unit.
 * Draw2.obj contributes no real code/data (only a 0-byte PSYLINK module-boundary marker);
 * this header exists so draw2.cpp has a sibling include (per-TU header convention). */
#ifndef _GAME_PSX_DRAW2_H_
#define _GAME_PSX_DRAW2_H_
#include "../../nfs4_types.h"
#endif /* _GAME_PSX_DRAW2_H_ */
