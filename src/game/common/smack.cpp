/* game/common/smack.cpp -- RECONSTRUCTED (empty runtime/library TU).
 *   GAME\COMMON\smack.obj -- empty marker (Smack_func @0x800B764C sz=0, .text). Smacker (RAD video) decoder stub; disabled in this build (NFS4 PSX uses Sony STR movies).
 *   Verified empty: no TUSummary row (or EXT=0/STAT=0) + ranges-map shows only a 0-4 byte
 *   PSYLINK boundary marker; no functions, no data attributed. Out of game-source scope
 *   (library/runtime), included for tree-inventory completeness. */
#include "smack.h"
/* (intentionally empty) */
/* end of smack.cpp */
